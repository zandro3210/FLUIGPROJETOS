function createDataset(fields, constraints, sortFields) {	var sql = 	" select "+"     p.num_proces,  "+"     p.log_ativ, "+"     ( "+"         select t.idi_status "+"         from tar_proces t "+"         where t.num_proces = p.num_proces "+"         and t.idi_status = 4 "+"     ) Status, "+"     dat_movto "+" from proces_workflow  p "+" inner join  histor_proces h "+" on p.num_proces = h.num_proces "+" where p.cod_def_proces = 'TransfCC' "+" and h.num_seq_movto_ant = 3 "+" and h.dat_movto >=" + fields[0] +" and h.dat_movto <= " + fields[1];		log.info("SQL " + sql);		var campos = new Array(sql);	var dataset = DatasetFactory.getDataset("ds_sql", campos, null, null);	return dataset;}