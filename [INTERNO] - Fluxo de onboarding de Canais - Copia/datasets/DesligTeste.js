function createDataset(fields, constraints, sortFields) {		// Cria o dataset	var newDataset = DatasetBuilder.newDataset();	newDataset.addColumn("retorno");    		// Conecta o servico e busca os livros	var periodicService = ServiceManager.getService('Desligamentos');	var serviceHelper = periodicService.getBean();	var serviceLocator =	serviceHelper.instantiate('org.tempuri.WsRequisicaoSSLLocator');	var service = serviceLocator.getServiceSoap();		// Invoca o servico	try {		//var retorno = service.obterDadosSecao("", fields[0], fields[1]); 		var retorno = service.getSchemaReqDesligamento(07, "rh","rh"); 		//var DocumentElement = new XML(retorno);      newDataset.addRow(retorno);		//newDataset.addRow(new Array("certo", "certo", "certo", //"certo"));			} catch(error) { newDataset.addRow(new Array(error.message, "erro", "erro", "erro")); }		return newDataset;	}