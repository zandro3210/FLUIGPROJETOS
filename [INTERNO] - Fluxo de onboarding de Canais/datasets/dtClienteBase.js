function createDataset(fields, constraints, sortFields) {		// Cria o dataset	var newDataset = DatasetBuilder.newDataset();	newDataset.addColumn("CODIGO");	newDataset.addColumn("RAZAO");    		// Conecta o servico e busca o cliente	var periodicService = ServiceManager.getService('wsOportunidade');		var serviceHelper  = periodicService.getBean();	var serviceLocator = serviceHelper.instantiate('br.com.totvs.ssimwsportal.CHAMTECLocator');	var service = serviceLocator.getCHAMTECSOAP();	// Invoca o servico	try {log.info(fields[0] + " == "  + fields[1]);		retorno = service.GETCLIENTES("",fields[0], fields[1]).LISTOFCLIENTES;				if(retorno){			for (var i = 0; i < retorno.length; i++){				newDataset.addRow(new Array(retorno[i].getCODIGO(),retorno[i].getRAZSOCIAL()));			}		}	} catch(error) { newDataset.addRow(new Array("erro",error.message)); }	return newDataset;}