function createDataset(fields, constraints, sortFields) { var cdOrigem = fields[0];//	var cdOrigem = "201142418142915";	var fields = new Array("nmItemRelacionado");		// Monta as constraints para consulta	var c1 = DatasetFactory.createConstraint("cdOrigem", cdOrigem, cdOrigem,ConstraintType.MUST);	var c2 = DatasetFactory.createConstraint("metadata#active", "true", "true",ConstraintType.MUST);	var constraints = new Array(c2,c1);	// Busca o dataset	var dataset = DatasetFactory.getDataset("brv_kgq_ir", fields, constraints,null);		return dataset;}